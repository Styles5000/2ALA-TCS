void PrintMenu(void) {
  // update this every time you add a new case
  // to the switch on userInput
  // Serial.println(F("a = test IR LED"));
  // Serial.println(F("b = test switches"));
  // Serial.println(F("c = test buttons"));
  // Serial.println(F("d = test motor & print left or right"));
  // Serial.println(F("e = test motor & IR sensor"));
  // Serial.println(F("f = test motor & Encoder"));
  // Serial.println(F("g = count stripes"));
  // Serial.println(F("h = test motion"));
  // Serial.println(F("i = Test Aim Fire"));
  // Serial.println(F("j = test reloader"));
  // Serial.println(F("k = Demo1()"));
  // Serial.println(F("s = get data from MATLAB"));
   Serial.println(F("t = compute distance from MATLAB data"));
  // Serial.println(F("v = Demo4 / comptition code"));
  Serial.println(F("v is for victory."));
  Serial.println(F("q is for quitters."));
  // Serial.println(F("z = print menu"));
  //Serial.println(F("done"));
} 
